# Self Agent

**Name**: self

## Description

Subagent that inherits the parent agent's full configuration including tools, system prompt, and model. Use it only when the orchestrator needs a focused follow-up in its own context.
